// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://shiva2cool.github.io', // REPLACE with your actual GitHub username if different
  base: '/', // Change this to '/chronolens/' if you are not using a top-level username.github.io repo
});
